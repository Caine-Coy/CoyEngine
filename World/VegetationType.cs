namespace CoyEngine
{
    /// <summary>
    /// Simple decorative vegetation layer (separate from terrain tiles).
    /// </summary>
    public enum VegetationType : byte
    {
        None = 0,
        Grass = 1,
        Forest = 2
    }
}
